ortools>=9.10
numpy>=1.26
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
httpx>=0.27
pytest>=7.4

# Accelerates the Monte Carlo simulator's inner loop (the field-vs-candidate
# comparison). Verified to produce bit-identical results to the vectorized
# NumPy fallback -- see test_numba_kernel_matches_numpy_fallback_exactly.
# dk_ev still works without it (SimulatorConfig.use_numba / a missing import
# fall back to pure NumPy), so it's safe to drop from this file in an
# environment where installing it is inconvenient.
numba>=0.59
